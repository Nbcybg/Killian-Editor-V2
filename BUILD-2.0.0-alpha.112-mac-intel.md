# BUILD — Killian 2 · 2.0.0-alpha.112 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.112-mac-intel.dmg` |
| ขนาด | 128 MB |
| SHA-256 | `ce440ed5db6bf1e8984c99d8db1a08d51ad6ea0c0b102fa7c0218639f2144566` |
| สร้างเมื่อ | 2026-08-26 16:44 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder 26.15.3 |
| สถาปัตยกรรม | x64 (Intel) — **ยังไม่ได้ทำ arm64 native** |
| ลายเซ็น | ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB **เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push** — ห้าม commit ลงสาขา
> ถ้าจะแจกจริงต้องใช้ GitHub Release (`gh release create`) หรือ Git LFS · repo เป็น public → **ถามก่อนทุกครั้ง**

---

## วิธีเปิดครั้งแรก (macOS)

ยังไม่ได้เซ็นใบรับรอง → macOS จะบล็อกไว้ครั้งแรก

1. **คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) แล้วกด Open ในกล่องที่เด้งขึ้นมา
2. ถ้ายังไม่ผ่าน เปิด Terminal แล้วรัน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## สร้างซ้ำ

```bash
npm run dist:mac
```

> **กับดัก:** `electron-builder` เขียนทับ `package.json` แล้วลบ `scripts`/`devDependencies`/`build`
> ทิ้งได้ (เกิดตอน `@electron/rebuild` รัน npm install ใน appDir) · **รอบ alpha.100 โดนจริง**
> และมันเขียนทับ *หลัง* คำสั่งจบไปแล้ว → **ต้องรอสักพักแล้วค่อยเช็ค ไม่ใช่ทันทีที่คำสั่งจบ**
> · **รีโปยังไม่ได้ `git init`** → ใช้ `git checkout HEAD -- package.json` ไม่ได้
>   ต้อง **ก๊อป `package.json` เก็บไว้ก่อน build แล้วเทียบ/กู้หลัง build** (รอบ .111 และ .112 ตรวจแล้ว **ไม่โดน**)

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **72 ไฟล์ · ผ่านครบ** (pdf-generator 119 ข้อ · +8 ของรอบนี้) |
| e2e (dev, `electron .`) | **1,912 PASS** → หยุดที่ `[104r]` (บั๊กค้าง **M-1** · ไม่ใช่ของรอบนี้) |
| **e2e ในตัว `.app` ที่บิลด์แล้ว** | **1,912 PASS · เหมือนกันเป๊ะทั้งจำนวนและจุดที่หยุด** |
| เช็คใหม่ของ alpha.112 | **4 ข้อ ผ่านครบ** ทั้งใน dev และในตัว `.app` |
| เช็คของ alpha.111 | **42 ข้อ ผ่านครบ** ทั้งสองที่ |
| `.dmg` เวอร์ชันใน `app.asar` | `2.0.0-alpha.112` |

ยืนยันว่าโค้ดรอบนี้อยู่ในบิลด์จริง (grep ใน `app.asar` ต้องใช้ `-a` เพราะเป็นไบนารี ·
**ข้อความไทยหาไม่เจอ** เพราะ esbuild escape เป็น `\uXXXX`):

`sp-blank` · `prevBlank` · `fabStackPositions` · `applyFmtbarConfig` เจอครบ
และ **ตัวกรองเก่าที่โยนบรรทัดว่างทิ้ง (`el !== "blank" && !omit`) หายไปแล้ว = 0**

---

## สิ่งที่เปลี่ยนจาก alpha.111

**แก้บั๊กร้ายแรง 2 ข้อที่ผู้ใช้รายงาน (ต้นตอเดียวกัน — ของค้างจาก alpha.86/87 ที่แก้ไม่ครบสองที่)**

1. **ส่งออก PDF ตัดหน้าไม่ตรงกับจอ** — `pdf-generator.js` ยังโยนบรรทัดว่างทิ้ง**ก่อน**จัดหน้า
   (ไฟล์จริงของผู้ใช้: จอ 2 หน้า · PDF ออกมา **1 หน้า**) และบรรทัดว่างที่ตั้งใจเว้นหายจากไฟล์หมด
   · `layoutPageLines()` ก็ต้องมิเรอร์กฎ `prevBlank` ของ `paginate()` ให้ครบด้วย
2. **มุมมองหน้าคู่เนื้อล้นออกนอกแผ่น** — กฎ CSS ของบรรทัดว่างจับด้วย `br.ProseMirror-trailingBreak`
   ซึ่งมีเฉพาะในตัวแก้ไข · มุมมองหน้าคู่/ภาพรวมประกอบ `<div class="sp sp-blank">` เอง จึงไม่โดนกฎ
   → บล็อกถัดจากบรรทัดว่างได้ระยะเว้นนำซ้ำ **ทุกช่องไฟกินเกินหนึ่งบรรทัด**
   (หน้าแรกของไฟล์ผู้ใช้: เนื้อสูง 1,248px ในแผ่นที่จุ 930px = เกิน 18 บรรทัด = จำนวนช่องไฟพอดี)

ดูรายละเอียด + ตัวเลขที่วัดได้ ใน `CHANGELOG.md` หัวข้อ **alpha.112**
