# BUILD — Killian 2 · 2.0.0-alpha.113 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.113-mac-intel.dmg` |
| ขนาด | 128 MB |
| SHA-256 | `b82a786e0fed564c1c06e61431499010fe6137c635c96d79f555ca4951db3146` |
| สร้างเมื่อ | 2026-08-26 17:49 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder 26.15.3 |
| สถาปัตยกรรม | x64 (Intel) · ลายเซ็น: ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push — ห้าม commit ลงสาขา

---

## วิธีเปิดครั้งแรก (macOS)

**คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) · ถ้ายังไม่ผ่าน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **72 ไฟล์ · 5,025 ข้อ · ผ่านครบ** |
| e2e (dev) | **1,916 PASS** → หยุดที่ `[104r]` (บั๊กค้าง **M-1** · ไม่ใช่ของรอบนี้) |
| **e2e ในตัว `.app` ที่บิลด์แล้ว** | **1,916 PASS · เหมือน dev เป๊ะทั้งจำนวนและจุดที่หยุด** |
| เช็คใหม่ alpha.113 | **4 ข้อ ผ่านครบ** ทั้งสองที่ |
| เช็ค alpha.112 | **4 ข้อ ผ่านครบ** ทั้งสองที่ |
| เวอร์ชันใน `app.asar` | `2.0.0-alpha.113` |

โค้ดรอบนี้อยู่ในบิลด์จริง (grep ใน `app.asar` · ต้องใช้ `-a`):
`modelHasScreenplay` · `keepBlanks` · `squashAll` · `sp-blank` เจอครบ

---

## สิ่งที่เปลี่ยนจาก alpha.112

**PDF มีสองไปป์ไลน์ — .112 แก้ไปแค่เส้นเดียว รอบนี้ปิดเส้นที่สอง**

| เส้น | ทางเดิน | แก้เมื่อ |
|---|---|---|
| กล่อง PDF ของแท็บบท | `blocksFromDoc` → `paginate` | alpha.112 |
| **ศูนย์รวมการส่งออก** (เมนู "ส่งออก PDF") | `buildDraftModel` → **`runWorkflow`** → `parseScript` | **alpha.113** |

ขั้นประกอบข้อความยุบ `\n{3,}` → `\n\n` ซึ่งถูกสำหรับนิยาย แต่ทำลายบท
(บรรทัดว่างในบท = เนื้อหาที่ `paginate()` นับเป็น 1 บรรทัด ตั้งแต่ alpha.86)

ไฟล์จริงของผู้ใช้ (A4 · ช่วงว่างติดกัน 3, 4, 7 บรรทัด): **41 บรรทัด → 28 → 1 หน้า**
ทั้งที่จอเห็น 2 หน้า · หลังแก้: **40 บรรทัด → 2 หน้า** ตรงกับจอ

**กับดักที่เกือบพลาด**: ตัดสินชนิดเอกสารด้วย "เสียงข้างมาก" (`docKind`) ไม่ได้ —
ฉบับร่างจริงของผู้ใช้มี 7 ฉาก เป็นบทแค่ฉากเดียว → โหวตออกมาเป็น "นิยาย"
ฉากบทฉากนั้นก็จะยังเสียบรรทัดว่างอยู่ดี · **ต้องตัดสินรายฉาก**

ดูรายละเอียดใน `CHANGELOG.md` หัวข้อ **alpha.113**
