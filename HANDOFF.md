# Killian 2 — ทำงานต่อจาก alpha.88

## สถานะปัจจุบัน
- **เป็น git repo ปกติ** (remote = `JabCrossHook/Killian_Editor`) — push ได้ตรง ๆ
- เวอร์ชัน `2.0.0-alpha.88` · สำรองจุดก่อนหน้าไว้ที่ `backup/master-alpha.87` (= `5e7d961`)
- เทส: **e2e ALL OK 3,258 checks · 0 FAIL** ทั้ง dev และ `dist/win-unpacked` · unit ครบ · `npm run i18n` ผ่าน

### วิธีรัน e2e บนเครื่องนี้ (Windows)
```bash
taskkill //F //IM electron.exe >/dev/null 2>&1
rm -f "C:/tmp/k2result.txt"; rm -rf "C:/Users/noobc/AppData/Local/Temp/k2proj"
node build.js && node test/fixture.js /tmp/k2proj \
  && KILLIAN_TEST=1 KILLIAN_TEST_PROJECT="C:/Users/noobc/AppData/Local/Temp/k2proj" \
     ./node_modules/.bin/electron .
# ผลอยู่ที่ C:/tmp/k2result.txt — รอบรรทัด "ALL OK" หรือ "STOP:"
```
ขับผ่าน CDP: `./node_modules/.bin/electron . --remote-debugging-port=9222`
(สคริปต์ตัวขับอยู่ในโฟลเดอร์ scratchpad ของ session — เขียนใหม่ได้ใน 20 บรรทัดด้วย `ws`)

---

## ✅ รายการจาก HANDOFF ของ alpha.87 — ปิดครบทุกข้อแล้วใน alpha.88

รายละเอียด "ต้นตอจริง" ของแต่ละข้ออยู่ใน [CHANGELOG.md](CHANGELOG.md) หัวข้อ alpha.88

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | นิยาย: ย่อหน้ายาวคร่อมหน้า เรนเดอร์เพี้ยน | ✅ `k-pb-inline` กว้าง 0 → เส้นประเริ่มกลางบรรทัด · โหมดจัดหน้าตัวหนังสือซ้อนกันด้วย |
| 2 | บทพูดตัดหน้าแสดง CONTINUED แทน (MORE) | ✅ ปิดไปแล้วใน .87 (ไม่ใช่บั๊ก) |
| 3 | มุมมองปกติของนิยายเป็นแผ่นแยก | ✅ ตัดสวิตช์ `paperGaps` ทิ้งทั้งชุด |
| 4 | บทหนังโชว์เลขหน้าที่หน้าแรก | ✅ กลับคำ .83r · เว้นเฉพาะ **หน้า 1 ของบทจริง ๆ** |
| 5 | หน้ากระดาษไม่ถูกล็อกความสูง | ✅ หางเลื่อนจอย้ายออกนอกกระดาษ + ล็อกเป็นจำนวนหน้าจริง |
| 6 | บล็อกเดียวยาวมาก ~100ms/ตัวอักษร | ✅ `deco-diff.js` — 22.4 → 15.1 ms (วัดที่ 23,026 อักขระ · decoration 1,000) |
| 7 | แปลงโหมดแล้วข้อความอาจหาย | ✅ **ไม่หาย** — 32,000 อักขระ ไป-กลับได้ไฟล์เดิมทุกไบต์ (วัดจากดิสก์) |
| 8 | วางบทดิบไม่ผ่านตัวจำแนก element | ✅ `SPEditor.pasteScript()` |

### รอบเก็บบั๊กเพิ่ม (แจ้งระหว่างทาง) — ปิดแล้วเช่นกัน
- **Enter ในบทหนังไม่ทิ้งระยะเว้นไว้ทีหลัง** — บล็อกใหม่โตจาก 1 → 3 บรรทัดตอนพิมพ์ตัวแรก
  ตอนนี้ `enter()` แทรกบรรทัดว่างนำเป็นบล็อกจริง (`blankLinesBefore()`)
- **CHANGELOG ในโปรแกรมค้างที่ alpha.75** — `renderer/CHANGELOG.md` เป็นสำเนาที่ก๊อปด้วยมือ
  ตอนนี้ `build.js` ก๊อปให้ทุกครั้ง + มีเทสกัน

---

## กฎที่ตกลงกันแล้ว — อย่ารื้อใหม่

1. **แยกเจ้าของความจริงของสองโหมด**
   - นิยาย: Chromium เป็นเจ้าของ (`prose-measure.js` วัด DOM จริง) → โมเดลตามจอ
   - บทหนัง: `sp-format.js` เป็นเจ้าของ → **จอตามโมเดล** (54 บรรทัด × ⅙ นิ้ว)
   - **กฎเหล็ก**: ห้ามเอาตัวเลขของบทมาจากการวัด DOM · ห้ามเอาตัวเลขของนิยายมาจากการทำนาย
2. `(CONTINUED)`/`CONTINUED:` = โอเวอร์เลย์ในระยะขอบ ไม่กินบรรทัด
   `(MORE)`/`ชื่อ+(cont'd)` = เนื้อบทจริง กินบรรทัด
3. **บล็อกที่ไม่มีข้อความ = บรรทัดว่าง 1 บรรทัด ทุกชนิด** (ยกเว้น `page-break`)
   ต้องตรงกัน 3 ที่: `lineFor()` · `.sp:has(> br.ProseMirror-trailingBreak)` · `blocksFromDoc()`
4. **ระยะเว้นบรรทัดเป็น `padding` บนกริด `--sp-line-h`** ไม่ใช่ `margin`
5. บล็อกแรกของทุกหน้าไม่มีระยะเว้นนำ (`paginate()` ให้ `before = 0`)
6. **[alpha.88] ระยะเว้นนำมีสองทางที่ให้ผลเท่ากัน** — บรรทัดว่างจริง (`prevBlank` ข้าม `linesBefore`)
   หรือ `linesBefore` · **ตอนกด Enter ใช้ทางบรรทัดว่างเสมอ** ไม่งั้นบล็อกโตตอนพิมพ์ตัวแรก
7. **มุมมองปกติ = เส้นประ · มุมมองจัดหน้า = แผ่นกระดาษจริง** (ห้ามเอาแบบหลังไปใส่แบบแรกอีก)

## ข้อจำกัดที่ผู้ใช้ย้ำ
- **JS ห้ามบวม** — ตรรกะใหม่ลง pure module (เทสด้วย node ได้) · app.js ได้แค่สายไฟ
  · **ทุกงานต้องหาของลบด้วย**
- **ไฟล์งาน (.md) ห้ามบวม** · ไทย 100% ทุก UI · ไฟล์เข้ากันได้กับ v1
- **ห้ามฮาร์ดโค้ดข้อความไทย** — ไปเพิ่มใน `languages/k2_*.csv` (`npm run i18n` เติมให้)

---

## บทเรียนของรอบนี้ (สำคัญกว่าตัวบั๊ก)

1. **e2e รันบนหน้าต่างจริง — ตำแหน่งเมาส์ของผู้ใช้ทำให้เทสแดงได้**
   `setupHoverTips()` ถอด `title` ออกจาก element ที่เมาส์ชี้อยู่ · เทส "รูปโชว์ชื่อตอน hover"
   จึงแดงทั้งที่โค้ดถูก · **พิสูจน์ด้วยการ `git stash` แล้วรัน HEAD เปล่า ๆ — แดงเหมือนกัน**
   → ก่อนโทษ diff ตัวเอง ให้เทียบกับ HEAD ก่อนเสมอ
2. **เทสที่พึ่ง "สภาพที่เทสก่อนหน้าทิ้งไว้" มีอยู่จริงหลายจุด** (เจอ 4 จุดในรอบนี้)
   บางตัวพิมพ์คำลงบล็อก *บรรยาย* แทน *ตัวละคร* มาตลอดแล้วผ่านเพราะบังเอิญ
   → เทสที่แตะเอกสารต้อง **ตั้งเอกสาร + เคอร์เซอร์เอง** และคืนสภาพด้วย **โหนดจริง**
   (`setMarkdown(getMarkdown())` **ไม่ใช่ identity** — บล็อกว่างที่มีชนิดกลายเป็น `action`)
3. **`gotoPos(1)` = ต้นบล็อก ซึ่งมีกติกา Enter ของตัวเอง** (ดันบรรทัดเดิมลง)
   จะทดสอบ "Enter สร้าง element ถัดไป" ต้องวางเคอร์เซอร์ **ท้ายบล็อก**
4. **`document.execCommand('selectAll')` ใช้ไม่ได้กับ ProseMirror** — ต้องส่ง Ctrl+A จริง
   ผ่าน `Input.dispatchKeyEvent` (เผลอใช้ทีนึงแล้ว วัดผิดไปหนึ่งรอบ)
5. **ก่อนเชื่อตัวเลขใด ๆ ให้ตรวจมาตรวัดก่อน** — เช่น `clientWidth` ของตัวแก้ไขรวมระยะขอบกระดาษ
   ด้วย เทียบความกว้าง widget กับมันจะผิดเสมอ (ต้องเทียบกับย่อหน้าที่มันอยู่)
6. **`npm run dist` ล้มเงียบได้** ถ้ามี electron ค้างจับไฟล์ (`EPERM: unlink dxil.dll`)
   → `taskkill` ให้หมด + `rm -rf dist/win-unpacked` ก่อน build เสมอ แล้วเช็ค `⨯` ในล็อก

---

## งานที่ยังเหลือ (ไม่ใช่บั๊กค้าง)

- **ข้อ 6 ต่อ**: ที่เหลือ ~15ms คือตัวสแกนเอง (regex ชื่อ Wiki + DP ตัดคำไทย วิ่งทั้งย่อหน้า
  ทุกครั้งที่พิมพ์) · จะเร็วกว่านี้ต้องแตะ "ช่วงที่สแกน" ซึ่งเคยพังมาแล้ว (decoration รั่ว)
  · **ตอนนี้มีประตูกันรั่วแล้ว** (`decoSignature()` + เทสเทียบเพิ่มทีละส่วน vs สแกนใหม่ทั้งเอกสาร)
  ถ้าจะลองรอบหน้าก็ปลอดภัยกว่าเดิมมาก
- **(CONTINUED) ต้องมีหัวฉาก** — ถ้าผู้ใช้อยากให้ขึ้นแม้ไม่มี `### หัวฉาก` ต้องผ่อนเงื่อนไข
  `p.sceneEnd > 0` ใน `annotateContinued()` (ยังไม่ได้ทำ — รอผู้ใช้ยืนยัน)
- ยังค้างจากก่อนหน้า: UI ของ search-engine / panel-ui / split-ui (ยังเป็น orphan module) ·
  multiple-drafts-per-book UI · screenplay align persistence · code signing · .ico/.icns
