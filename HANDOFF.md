# Killian 2 — ทำงานต่อจาก alpha.87

## สถานะปัจจุบัน
- **เป็น git repo ปกติ** (remote = `JabCrossHook/Killian_Editor`) — push ได้ตรง ๆ ไม่ต้อง clone ไป /tmp
- เวอร์ชัน `2.0.0-alpha.87` · master = `3f26ad1` · working tree สะอาด
- สำรองไว้: `backup/master-alpha86-2026-08-19` (f6b4cf5 = จุดก่อน .87)
- เทส: **e2e ALL OK 3,231 checks · 0 FAIL** · unit ผ่านครบ · บิลด์ `dist/Killian2-2.0.0-alpha.87-portable.exe`

### วิธีรัน e2e บนเครื่องนี้ (Windows)
```bash
mkdir -p /c/tmp && rm -f /c/tmp/k2result.txt
node test/fixture.js "C:/tmp/k2proj"
KILLIAN_TEST=1 KILLIAN_TEST_PROJECT="C:/tmp/k2proj" ./node_modules/.bin/electron .
# ผลอยู่ที่ /c/tmp/k2result.txt (เขียนเรื่อย ๆ ระหว่างรัน — ต้องรอบรรทัด "ALL OK" หรือ "STOP:")
```
เปิดแอปพร้อมดีบักเกอร์: `./node_modules/.bin/electron . --remote-debugging-port=9222`
แล้วขับผ่าน CDP (มีสคริปต์ตัวอย่างในโฟลเดอร์ scratchpad ของ session ก่อน)

---

## กฎที่ตกลงกันแล้ว — อย่ารื้อใหม่

1. **แยกเจ้าของความจริงของสองโหมด**
   - นิยาย: Chromium เป็นเจ้าของ (`prose-measure.js` วัด DOM จริง) → โมเดลตามจอ
   - บทหนัง: `sp-format.js` เป็นเจ้าของ → **จอตามโมเดล** (บทนิยามหน้าด้วย 54 บรรทัด × ⅙ นิ้ว)
   - ใช้ร่วมกันได้: ขนาดกระดาษ · ระยะขอบ · ความกว้างตัวอักษร เท่านั้น
   - **กฎเหล็ก**: ห้ามเอาตัวเลขของบทมาจากการวัด DOM · ห้ามเอาตัวเลขของนิยายมาจากการทำนาย
2. `(CONTINUED)`/`CONTINUED:` = โอเวอร์เลย์ในระยะขอบ ไม่กินบรรทัด
   `(MORE)`/`ชื่อ+(cont'd)` = เนื้อบทจริง กินบรรทัด
3. **บล็อกที่ไม่มีข้อความ = บรรทัดว่าง 1 บรรทัด ทุกชนิด** (ยกเว้น `page-break`)
   นิยามนี้ต้องตรงกัน 3 ที่: `lineFor()` (ไฟล์) · `.sp:has(> br.ProseMirror-trailingBreak)` (จอ) ·
   `blocksFromDoc()` (โมเดล)
4. **ระยะเว้นบรรทัดเป็น `padding` บนกริด `--sp-line-h`** ไม่ใช่ `margin` (margin ยุบเป็น max())
   ค่าอยู่ในตัวแปร `--k-pad` ที่ `spCss()` สร้าง — **widget ที่วางแบบ absolute ในบล็อกต้องอ่านค่านี้ด้วย**
   (เลขฉากเคยลอยคนละบรรทัดเพราะลืมข้อนี้)
5. บล็อกแรกของทุกหน้าไม่มีระยะเว้นนำ (`paginate()` ให้ `before = 0` — ระยะขอบกระดาษทำหน้าที่นั้นแล้ว)

## ข้อจำกัดที่ผู้ใช้ย้ำ
- **JS ห้ามบวม** — `app.js` ~26,000 บรรทัด · ตรรกะใหม่ลง pure module (เทสด้วย node ได้)
  app.js ได้แค่สายไฟ · **ทุกงานต้องหาของลบด้วย**
- **ไฟล์งาน (.md) ห้ามบวม** — ห้ามยัดสำเนาเนื้อหาลง frontmatter
- ไทย 100% ทุก UI · ไฟล์เข้ากันได้กับ v1

## วิธีทำงานที่ผู้ใช้คาดหวัง
**reproduce ก่อนเดา** (เขียนสคริปต์เรียกเอนจินจริง / วัดใน DOM จริงผ่าน CDP) → แก้ root cause
→ **เพิ่ม unit + e2e ถาวร** → `npm run test:unit` → e2e ALL OK → bump version + CHANGELOG + README
→ `npm run dist` → ตรวจ asar → push

> **บทเรียนจาก session ที่แล้ว — สำคัญมาก**
> ผมพลาดหลายครั้งเพราะ **เชื่อมาตรวัดของตัวเองโดยไม่ตรวจมันก่อน**:
> - วัดความสูงบล็อกโดยลืมหาร `zoom` → สรุปผิดว่า "ทุกบล็อกผิดกริด" ทั้งที่ถูกหมด
> - เทียบ `textContent.length` ข้ามโหมด ทั้งที่โหมดบทนับ widget เลขฉากเป็นข้อความด้วย
> - ตั้งชนิดบล็อกในเทสผิด index แล้วสรุปว่า "(MORE) ไม่ทำงาน"
> - แก้ e2e ให้ผ่านโดยยังไม่ได้ตรวจว่ามันวัดของจริงหรือเปล่า
>
> **ก่อนเชื่อตัวเลขใด ๆ ให้ตรวจมาตรวัดก่อนเสมอ** (เช็ค zoom · เช็คว่า element ที่วัดคือตัวที่ตั้งใจ ·
> เช็คว่าเอกสารทดสอบสะอาดจริง — `document.execCommand('selectAll')` **ใช้ไม่ได้** กับ ProseMirror
> ต้องส่งคีย์ Ctrl+A จริงผ่าน `Input.dispatchKeyEvent`)

---

# งานที่ยังไม่ได้แก้ (เรียงตามที่ผู้ใช้แจ้ง)

## 1 · นิยาย: บรรทัดยาวจนตัดหน้า → เรนเดอร์เพี้ยน  🔴 ยังไม่ได้เริ่ม

ผู้ใช้: *"ในนิยายนะ เมื่อบรรทัดที่ยาวมากๆ จนตัดหน้า ระบบตัดหน้าจะ render ออกมา error เลย
จะไม่มีการใช้ canvas เปล่าคั่นระหว่างหน้า เส้นประขอบระยะ margin ล่างจะขึ้นมาบังตัวหนังสือ"*

**สิ่งที่ต้องหา**: ย่อหน้าเดียวที่ยาวจนคร่อมรอยต่อหน้าในโหมดนิยาย —
เส้นคั่นหน้า (`.ed-page-break`) ถูกวาดกลางย่อหน้า แล้วเส้นประระยะขอบล่างทับตัวหนังสือ
ดู `src/prose-view.js` (ตัววาดเส้นคั่น) + `src/prose-measure.js` (หาจุดตัดจากการวัด DOM)

repro: โหมดนิยาย → ย่อหน้าเดียวยาว ~5,000 อักขระ ไม่มีช่องว่าง → ดูรอยต่อหน้า

## 2 · บทพูดตัดหน้าแสดง CONTINUED แทน (MORE)  ✅ ปิดเคส — ไม่ใช่บั๊ก

**ผู้ใช้หาเจอเอง: เอกสารทดสอบ "ไม่ได้ใส่ชื่อตัวละคร"** — มีแต่บล็อกบทพูดลอย ๆ

ยืนยันในโค้ดแล้ว `src/sp-format.js:536`
```js
const dlgSplit = isDlg && wantDlgMarkers && !!lastChar;   // lastChar มาจากบล็อก character เท่านั้น
const moreLines = dlgSplit ? 1 : 0;
```
ไม่มีชื่อตัวละคร → `dlgSplit` เท็จ → ไม่มี `(MORE)` และไม่มี `ชื่อ (cont'd)`
แล้ว `annotateContinued()` ไม่เห็น `more`/`contd` จึงใส่ CONTINUED ของฉากแทน = ตรงกับรูปที่ผู้ใช้ส่งมา

**เป็นการป้องกันที่ถูกต้อง** — เขียน `(cont'd)` โดยไม่มีชื่อไม่ได้ จะได้บรรทัดว่างเปล่าใน PDF

**และแอปเตือนอยู่แล้ว**: `sp-validator.js:108` มีกฎ `ORPHAN_DIALOGUE`
("บทพูดไม่มีชื่อตัวละคร") ระดับ **error** โผล่ที่ป้ายจำนวนข้อผิดพลาดบนแถบสถานะ

### สิ่งที่ *อาจ* ยังควรทำ (ถามผู้ใช้ก่อน)
คำเตือนอาจไม่เด่นพอ — ผู้ใช้เจอ CONTINUED ผิดที่ก่อนจะเห็นป้ายเตือน
ทางเลือก: (ก) ปล่อยไว้ (ข) ทำป้ายเตือนให้เด่นขึ้น (ค) ขีดเส้นใต้บล็อกบทพูดกำพร้าในตัวแก้ไข
**ยังไม่ได้ตัดสินใจ — อย่าแก้เองโดยไม่ถาม**

## 3 · มุมมองปกติของนิยายแสดงเป็นแผ่นแยก  🔴 ยังไม่ได้เริ่ม

ผู้ใช้: *"ใน mode นิยาย มุมมองปกติ กลับเป็นมุมมองที่ถูกต้องของมุมมอง layout ซึ่งมันผิด
มุมมองปกติต้องแบ่งหน้าด้วยเส้นประ ไม่ใช่เหรอ ไม่มี canvas ว่างแบ่งหน้า"*

= โหมดนิยาย มุมมอง **ปกติ** ต้องเป็นสายเนื้อหาต่อเนื่อง คั่นหน้าด้วย**เส้นประ**
ไม่ใช่แผ่นกระดาษแยกที่มีช่องว่างคั่น (นั่นคือหน้าที่ของมุมมอง **จัดหน้า**)
ดู `src/prose-view.js` + CSS `.pane` ของโหมดนิยาย

## 4 · โหมดบทหนัง มุมมองปกติโชว์เลขหน้าที่หน้าแรก  🔴 ยังไม่ได้เริ่ม

หน้าแรกไม่ควรมีเลขหน้า (มาตรฐานบท) · ดู `setSpPageNumberLabel()` / `pageNumberLabelFor()` ใน `app.js`
และ `updatePageNumberHint()` — มี `startPage` ต่อไฟล์ด้วย ระวังอย่าทำของเดิมพัง

## 5 · หน้ากระดาษไม่ถูกล็อกความสูง ยืดหดได้  🔴 ยังไม่ได้เริ่ม

ผู้ใช้ส่งรูป: แผ่นกระดาษสูงผิดปกติ มีที่ว่างมหาศาลใต้เนื้อหา
*"หน้ากระดาษไม่เคยถูก lock เลย ยืดหดได้ตลอด เกิดจากอะไร"*

**เบาะแส**: `setLayoutPageCount()` ตั้ง `--pg-count` แล้ว CSS ใช้เป็น `min-height` ของพื้นที่
ถ้า `min-height` ยังอยู่ที่ไหนสักแห่ง แผ่นจะยืดตามเนื้อหาแทนที่จะคงที่
(alpha.87 เปลี่ยนกระดาษในมุมมองจัดหน้าเป็น `height` ตายตัวไปแล้ว — แต่มุมมองอื่นอาจยังไม่ได้)
ตรวจทุกที่ที่มี `min-height` เกี่ยวกับกระดาษใน `renderer/style.css`

## 6 · ความเร็ว: บล็อกเดียวที่ยาวมาก ยังเหลือ ~100ms/ตัวอักษร  🟡 แก้ไปครึ่งทาง

**ทำไปแล้ว**: `fitPrefix()` เปลี่ยนเป็นหาหน้าต่างแบบทวีคูณก่อน binary search
→ 815ms → 107ms (7.6 เท่า) · `measureText` ลงจาก 57.2% เหลือ 4.1%

**ที่เหลือ**: `removeInner` **59%** — ProseMirror ลบ/ใส่ decoration ของตัวตรวจคำผิด
**2,252 ตัว** ในบล็อกยักษ์ใหม่ทุกครั้งที่พิมพ์ (`incrementalDecoState` ใน `src/editor.js`)

**เคยลองแล้วล้มเหลว — อย่าทำซ้ำแบบเดิม**: จำกัดให้สแกนเฉพาะหน้าต่างรอบจุดที่แก้
(`scanWindow`/`snapOut`) → **decoration รั่วสะสม** งานโตจาก 58ms เป็น 601ms ต่อเนื่อง
สาเหตุที่สงสัย: `moved.find(from, to)` กับ `scan(from, to)` มีนิยาม "ทับช่วง" ไม่ตรงกัน
→ ของที่คร่อมขอบหน้าต่างถูกใส่ซ้ำ **ถ้าจะทำใหม่ต้องมีเทสนับจำนวน decoration ต่อการพิมพ์หนึ่งตัว
ว่าคงที่ ไม่โต** ก่อนวัดความเร็ว

**หมายเหตุ**: เอกสารที่แบ่งบล็อกตามปกติ (44,288 อักขระ · 600 บล็อก · 28 หน้า) = **13ms** ลื่นมาก
ปัญหานี้เกิดเฉพาะย่อหน้าเดียวที่ยาวหลายหมื่นอักขระ

## 7 · แปลงโหมดในแอปแล้วข้อความอาจหาย  🟡 ยังไม่ได้ข้อสรุป

ผู้ใช้: *"เหมือนคุณไปจำกัดจำนวนตัวหนังสือต่อ block พอแปลงไป mode บทภาพยนตร์ ข้อความเลยถูกลบ"*

**ตรวจแล้ว**: ตัวแปลงบริสุทธิ์ `convertBody()` **ไม่ทำข้อความหายเลย** ทดสอบที่
100 / 1,000 / 5,000 / 20,000 / 38,000 อักขระในย่อหน้าเดียว → ไป-กลับครบทุกขนาด

**แต่ยังสรุปไม่ได้** ว่าในแอปหายจริงไหม เพราะมาตรวัดที่ใช้เชื่อไม่ได้
(โหมดบทนับ widget เลขฉากเป็น `textContent` ด้วย → เทียบข้ามโหมดไม่ตรง)

**วิธีวัดที่ถูก**: เทียบ **ไฟล์ .md บนดิสก์** ก่อน/หลังสลับโหมด ด้วย `kapi.readFile()`
หรือเทียบ `getMarkdown()` ไม่ใช่ `textContent`

## 8 · วางข้อความบทดิบลงตัวแก้ไข ไม่ผ่านตัวจำแนก element  🔴 เจอระหว่างทดสอบ ยังไม่ได้แก้

วาง `### ฉาก 1` ลงตัวแก้ไขบทหนัง → ได้บล็อก **บรรยาย** ที่มีข้อความ `### ฉาก 1`
แทนที่จะเป็น **หัวฉาก** ที่มีข้อความ `ฉาก 1`

ตอนเปิดไฟล์ `spDocFromMarkdown()` จำแนกถูก แต่ทางการวางไม่ได้ใช้ตัวเดียวกัน
ควรให้ handler ของ paste ส่งข้อความผ่าน `parseScript()` ของ `fountain.js` เหมือนกัน
(น่าจะเป็นสาเหตุที่ผู้ใช้เจอปัญหา (MORE) ในข้อ 2 ด้วย ถ้าบทมาจากการวาง)

---

## เริ่มยังไงดี

แนะนำเรียงแบบนี้ (ผลกระทบต่อผู้ใช้ ÷ ความเสี่ยง):

1. **ข้อ 8** — เล็ก ชัด (ข้อ 2 ปิดเคสไปแล้ว: ผู้ใช้ไม่ได้ใส่ชื่อตัวละคร ไม่ใช่บั๊ก)
2. **ข้อ 5** (กระดาษยืด) + **ข้อ 3** (มุมมองปกติของนิยาย) — เป็นเรื่อง CSS/มุมมอง วัดง่าย เห็นผลทันที
3. **ข้อ 4** (เลขหน้าแรก) — เล็ก
4. **ข้อ 1** (นิยายตัดหน้ากลางย่อหน้ายาว)
5. **ข้อ 7** (วัดใหม่ให้ถูก) แล้วค่อยตัดสินว่ามีบั๊กจริงไหม
6. **ข้อ 6** (ความเร็วที่เหลือ) — เสี่ยงสุด ต้องมีเทสกันรั่วก่อน

**ขอไฟล์ .md ของฉากที่ผู้ใช้เจอปัญหา** จะช่วยข้อ 7 ได้มากที่สุด
