# BUILD — Killian 2 · 2.0.0-alpha.102 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.102-mac-intel.dmg` |
| ขนาด | 128 MB |
| SHA-256 | `a30e9d57b83cf3d0773c481a805f8bf865374b24d8401543814e2016c4947e3a` |
| สร้างเมื่อ | 2026-08-25 19:01 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder 26.15.3 |
| สถาปัตยกรรม | x64 (Intel) — **ยังไม่ได้ทำ arm64 native** |
| ลายเซ็น | ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB **เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push** — ห้าม commit ลงสาขา
> ถ้าจะแจกจริงต้องใช้ GitHub Release (`gh release create`) หรือ Git LFS · repo เป็น public → **ถามก่อนทุกครั้ง**

---

## วิธีเปิดครั้งแรก (macOS)

ยังไม่ได้เซ็นใบรับรอง → macOS จะบล็อกไว้ครั้งแรก

1. **คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) แล้วกด Open ในกล่องที่เด้งขึ้นมา
2. ถ้ายังไม่ผ่าน เปิด Terminal แล้วรัน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## สร้างซ้ำ

```bash
npm run dist:mac
```

> **กับดัก:** `electron-builder` เขียนทับ `package.json` แล้วลบ `scripts`/`devDependencies`/`build`
> ทิ้งได้ (เกิดตอน `@electron/rebuild` รัน npm install ใน appDir) · **รอบ alpha.100 โดนจริง**
> และมันเขียนทับ *หลัง* คำสั่งจบไปแล้ว → **ต้องรอสักพักแล้วค่อยเช็ค ไม่ใช่ทันทีที่คำสั่งจบ**
> สำรองไฟล์ไว้ก่อนเสมอ (ถ้าอยู่ใน git ใช้ `git checkout HEAD -- package.json` หลัง build)
> รอบ alpha.101 และ alpha.102 ตรวจแล้ว **ไม่โดน**

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **70 ไฟล์ · 4,814 ข้อ · ผ่านครบ** |
| e2e (dev, `electron .`) | **3,703 checks · ALL OK** |
| **e2e ในตัว `.app` ที่บิลด์แล้ว** | **3,703 checks · ALL OK** |
| `.dmg` เวอร์ชันใน Info.plist | `2.0.0-alpha.102` |
| stderr ของ `.app` | ไม่มี crash |

ยืนยันว่าโค้ดรอบนี้อยู่ในบิลด์จริง (grep ใน `app.asar`):
`KEEP_WITH_NEXT` · `hasContentAfter` · `keepSceneWithNext` เจอครบ

---

## สิ่งที่เปลี่ยนจาก alpha.101

1. **เลขฉากอยู่บรรทัดเดียวกับหัวฉากเสมอ** — เดิมหัวฉากที่เป็นบรรทัดบนสุดของหน้า เลขจะหล่นไป
   บรรทัดถัดไป เพราะกฎ "บล็อกแรกของหน้าไม่มีระยะเว้นนำ" ตัด `padding-top` แต่ลืมตัด `--k-pad`
   ซึ่งเป็นตัวแปรที่ widget เลขฉากใช้เป็น `top` (วัดจริงหลังแก้: ห่าง **0px** ทั้งหัวฉากแรกของหน้า
   และหัวฉากกลางเอกสาร)
2. **หัวเรื่องไม่ค้างเป็นบรรทัดสุดท้ายของหน้า** — หัวฉาก · ทรานซิชัน · ฉากย่อย · สลับฉาก · shot ·
   องก์ · โน้ต · ต่อเนื่องซ้าย/ขวา · ทรานซิชันเข้า (10 ชนิด)
   · กฎ `keepSceneWithNext` **มีในตารางและในกล่องตั้งค่ามาตั้งแต่ข้อ 84 แต่ `paginate()`
     ไม่เคยอ่านมันเลย** — ตอนนี้ทำงานแล้ว ปรับได้ที่ ตั้งค่า → หน้ากระดาษ (ตั้ง 0 = ปิด)
   · ยกเว้นตัวที่เป็นบล็อกสุดท้ายของเอกสารจริง ๆ (เช่น `FADE OUT.` ปิดเรื่อง) — ไม่ควรเสียทั้งหน้า
     เพื่อบรรทัดเดียว

ดูรายละเอียด + บันทึกการไล่บั๊ก (สองสมมติฐานที่วัดแล้วพบว่าไม่ใช่) ใน `CHANGELOG.md` หัวข้อ **alpha.102**
