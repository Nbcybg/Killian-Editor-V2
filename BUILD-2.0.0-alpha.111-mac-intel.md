# BUILD — Killian 2 · 2.0.0-alpha.111 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.111-mac-intel.dmg` |
| ขนาด | 128 MB (134,380,797 ไบต์) |
| SHA-256 | `0d20cf4e6f7ad2e6f7eeefac91bc16f8783c95fd529409b2ef1a924076796084` |
| สร้างเมื่อ | 2026-08-26 15:51 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder 26.15.3 |
| สถาปัตยกรรม | x64 (Intel) — **ยังไม่ได้ทำ arm64 native** |
| ลายเซ็น | ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB **เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push** — ห้าม commit ลงสาขา
> ถ้าจะแจกจริงต้องใช้ GitHub Release (`gh release create`) หรือ Git LFS · repo เป็น public → **ถามก่อนทุกครั้ง**

---

## วิธีเปิดครั้งแรก (macOS)

ยังไม่ได้เซ็นใบรับรอง → macOS จะบล็อกไว้ครั้งแรก

1. **คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) แล้วกด Open ในกล่องที่เด้งขึ้นมา
2. ถ้ายังไม่ผ่าน เปิด Terminal แล้วรัน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## สร้างซ้ำ

```bash
npm run dist:mac
```

> **กับดัก:** `electron-builder` เขียนทับ `package.json` แล้วลบ `scripts`/`devDependencies`/`build`
> ทิ้งได้ (เกิดตอน `@electron/rebuild` รัน npm install ใน appDir) · **รอบ alpha.100 โดนจริง**
> และมันเขียนทับ *หลัง* คำสั่งจบไปแล้ว → **ต้องรอสักพักแล้วค่อยเช็ค ไม่ใช่ทันทีที่คำสั่งจบ**
> · **รอบนี้รีโปยังไม่ได้ `git init`** → ใช้ `git checkout HEAD -- package.json` ไม่ได้
>   ต้อง **ก๊อป `package.json` เก็บไว้ก่อน build แล้วเทียบ/กู้หลัง build** (รอบ alpha.111 ตรวจแล้ว **ไม่โดน**)

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **72 ไฟล์ · 5,010 ข้อ · ผ่านครบ** |
| e2e (dev, `electron .`) | **1,908 PASS** → หยุดที่ `[104r]` (บั๊กค้าง **M-1** · ไม่ใช่ของรอบนี้) |
| **e2e ในตัว `.app` ที่บิลด์แล้ว** | **1,908 PASS · เหมือนกันเป๊ะทั้งจำนวนและจุดที่หยุด** |
| เช็คใหม่ของ alpha.111 | **42 ข้อ ผ่านครบทั้งใน dev และในตัว `.app`** |
| `.dmg` เวอร์ชันใน `app.asar` | `2.0.0-alpha.111` |
| stderr / log ของ `.app` | ไม่มี crash · **0 error/warn** ตั้งแต่เปิดแอป (เปิดผลงานจริงของผู้ใช้ กู้ 7 แท็บได้ปกติ) |

> **หมายเหตุเรื่อง e2e ที่ยังไม่ ALL OK**: `[104r] note: โมเดลนับบรรทัดตรงกับจอ` (125 vs 134)
> เป็นบั๊กค้าง **M-1** ที่มีอยู่ก่อนรอบนี้ (ดูตาราง Known issues ใน `CHANGELOG.md`) —
> `transition` (ตัวปกติ) ตรงเป๊ะ 534/534 ส่วน `note` (ตัวเอียง) คลาด · ไม่เกี่ยวกับ FAB/แถบรูปแบบ

ยืนยันว่าโค้ดรอบนี้อยู่ในบิลด์จริง (grep ใน `app.asar` · ต้องใช้ `-a` เพราะ asar เป็นไบนารี
และ **ข้อความไทยหาไม่เจอ** เพราะ esbuild escape เป็น `\uXXXX`):

`fabRadialPositions` · `fabStackPositions` · `fmtBarTarget` · `applyFmtbarConfig` ·
`tb-fmt-here` · `k-fab-item` · `fmtbar-here` · `crosshair` · `ui.fab.actScene` — เจอครบ

---

## สิ่งที่เปลี่ยนจาก alpha.110

1. **ปุ่มลอย (FAB) ย้ายได้ด้วยคลิกขวาค้างแล้วลาก** + จำตำแหน่ง · คลิกขวาแล้วไม่ขยับ = เมนูคำสั่งลัด
2. **เลือกคำสั่งบน FAB เองได้สูงสุด 4 จาก 23 คำสั่ง** + เลือกรูปแบบ (ไอคอน / ไอคอน+ข้อความ / ข้อความ)
3. **เมนู FAB กางแบบ radial ของ Material** (แบบมีข้อความเรียงแถวตั้ง กันป้ายทับกัน)
4. **แถบรูปแบบลอยคลิกขวาปรับแต่งได้ · เก็บคนละชุดระหว่างนิยายกับบทภาพยนตร์**
5. **ปุ่ม "เรียกแถบมาหาเคอร์เซอร์"** (`Ctrl+Shift+/`) ย้ายแบบ tween · นอกพื้นที่เขียน → กลางจอ ต่ำกว่ากลาง 250px
6. **ปุ่มที่โหมดนั้นใช้ไม่ได้เป็นสีเทาและกดไม่ติดจริง** (บท: หัวข้อ · ยกคำพูด · ตัวยก · ตัวห้อย)

ของแถม: คีย์ลัด `Ctrl+Alt+1..5` (สร้างบท/ฉาก/ตัวละคร/สถานที่/memo) ที่มีในตารางมาตั้งแต่ alpha.79
แต่ **ไม่เคยมีตัวรับใน `handleCommand`** — กดแล้วเงียบมาตลอด ตอนนี้ทำงานแล้ว

ดูรายละเอียดทั้งหมดใน `CHANGELOG.md` หัวข้อ **alpha.111**
