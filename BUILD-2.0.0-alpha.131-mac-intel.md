# BUILD — Killian 2 · 2.0.0-alpha.131 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.131-mac-intel.dmg` |
| ขนาด | 128 MB |
| SHA-256 | `c39f344122193046ee5e0f6d65a1cb3d461915aa110eea8531c792d4e8af7ea9` |
| สร้างเมื่อ | 2026-09-04 18:02 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder |
| สถาปัตยกรรม | x64 (Intel) · ลายเซ็น: ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push — **ห้าม commit ลงสาขา**

---

## วิธีเปิดครั้งแรก (macOS)

**คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) · ถ้ายังไม่ผ่าน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **87 ไฟล์ · 5,573 ข้อ · ผ่านครบ** |
| e2e (`KILLIAN_TEST=1`) | **4,299 ข้อ · `ALL OK`** |
| smoke test | เปิด `.app` จริง · ครบทั้ง `--type=renderer` / `gpu` / `utility` · log ไม่มี error/unhandled |
| ยืนยันโค้ดใหม่อยู่ในบิลด์ | `max(var(--k-pb-strip)` พบใน `app.asar` |

> ⚠️ **กับดักตอน smoke test (เจอในรอบนี้)**: อินสแตนซ์เก่าที่ยังค้างอยู่ทำให้ `open` ไป
> **โฟกัสตัวเดิม** แทนที่จะเปิดบิลด์ใหม่ — เห็นเป็น "ไม่มี `--type=renderer`" แล้วเกือบสรุปว่า
> บิลด์พัง · ฆ่าให้เหลือ 0 ก่อนทุกครั้ง แล้วยืนยันเวลาเริ่มของโปรเซสด้วย `ps -o lstart=`

## มีอะไรใหม่

**ปิดเคส P-1** — รอยต่อหน้าในมุมมองปกติเท่ากันทุกหน้าแล้ว

เดิมหน้าที่ตามหลัง "จุดตัดกลางย่อหน้า" ยาวเกินไป **30px = หนึ่งบรรทัดพอดี** ทุกครั้ง
เพราะแถบหนึ่งบรรทัดของเส้นคั่นถูกวางไว้ *ใต้* รอยต่อหน้า จึงไปกินที่ของหน้าถัดไป
· แก้ที่ CSS: `height:calc(strip + pad)` → `height:max(strip, pad)` + ย้ายเส้นประลงก้นกล่อง

> มุมมอง "จัดหน้า" (แผ่นกระดาษจริง) **ไม่เคยผิดเลย** — ตัวเลขที่รายงานไว้ใน alpha.130
> (1112/1056/1084) มาจากการวัด **ขอบบนกล่อง** ซึ่งสูงไม่เท่ากันตามชนิดอยู่แล้ว
> ไม่ใช่การวัดรอยต่อหน้า

เหลือ 6px เฉพาะหน้าที่เต็มจนที่ว่างท้ายหน้าไม่ถึงหนึ่งบรรทัด — แก้ให้หายสนิทต้องให้ตัวจัดหน้า
ตัดเร็วขึ้นหนึ่งบรรทัด ซึ่งจะทำให้จอกับ PDF ตัดหน้าคนละที่ จึง **ตั้งใจไม่แก้** (มีเทสคุมไว้ว่า
เกินได้เฉพาะกรณีนี้กรณีเดียว)

ดู [CHANGELOG.md](CHANGELOG.md) หัวข้อ alpha.131

## สร้างซ้ำ

```bash
cd "/Users/kaipleng/Desktop/Killian_Editor-master 2"
npm run dist:mac
git checkout HEAD -- package.json    # electron-builder เขียนทับ package.json — ต้องคืนทุกครั้ง
```
