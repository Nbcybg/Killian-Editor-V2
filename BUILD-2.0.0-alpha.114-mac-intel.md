# BUILD — Killian 2 · 2.0.0-alpha.114 · macOS Intel (x64)

| | |
|---|---|
| ไฟล์ | `dist/Killian2-2.0.0-alpha.114-mac-intel.dmg` |
| ขนาด | 128 MB |
| SHA-256 | `d7ca0a52ad83851b97a1bc6b70121823f4f2a95af42b098eaa42c2e06d386ebd` |
| สร้างเมื่อ | 2026-08-26 18:44 (เครื่อง Top · macOS 24.6.0) |
| Electron | 43.2.0 · electron-builder 26.15.3 |
| สถาปัตยกรรม | x64 (Intel) · ลายเซ็น: ไม่มี (`identity: null`) · ไม่มีไอคอน `.icns` |

> ⚠️ `.dmg` 128 MB เกินขีดจำกัด 100 MB/ไฟล์ ของ GitHub push — **ห้าม commit ลงสาขา**

---

## วิธีเปิดครั้งแรก (macOS)

**คลิกขวาที่ `Killian 2.app` → Open** (ไม่ใช่ดับเบิลคลิก) · ถ้ายังไม่ผ่าน:

```bash
xattr -cr "/Applications/Killian 2.app"
```

---

## ผลการทดสอบก่อนปล่อย

| ชุด | ผล |
|---|---|
| unit | **72 ไฟล์ · 5,025 ข้อ · ผ่านครบ** |
| **e2e (dev)** | **ALL OK · 3,836 PASS** ← ผ่านครบทั้งชุดครั้งแรกในรอบ .111–.114 |
| **e2e ในตัว `.app` ที่บิลด์แล้ว** | **ALL OK · 3,836 PASS** (เท่ากับ dev เป๊ะ) |
| เวอร์ชันใน `app.asar` | `2.0.0-alpha.114` |
| โค้ดรอบนี้อยู่ในบิลด์ | `linear-gradient(to right, #ffc107` เจอใน asar |

> **บั๊กค้าง M-1 ปิดเคสแล้ว** — ก่อนหน้านี้ e2e หยุดที่ `[104r]` ที่ 1,916 PASS ทุกครั้ง

---

## สิ่งที่เปลี่ยนจาก alpha.113

รอบนี้ไม่มีฟีเจอร์ใหม่ — ไล่ปิดของค้างจนชุดเทสเดินจบเส้น แล้วเจอของที่ถูกบังไว้อีกสองชั้น

1. **ปิด M-1** — `.sp-note` มี `padding:4px 8px` + `border-left:3px` บน `box-sizing:border-box`
   กินความกว้างข้อความไป **19px พอดี** · แยกปัจจัยด้วยการวัดสามกรณี
   (บรรยายปกติ 54/54 · บรรยาย**บังคับเอียง** 54/54 · โน้ต 54/61) → **ตัวเอียงไม่เกี่ยวเลย**
   แก้ด้วยแถบสีแบบ background gradient แทน border + ตัด padding แนวนอน
   \+ วางตาข่ายที่กวาด **ทุกชนิดบล็อก** ว่าความกว้างเนื้อในบนจอ = ความกว้างที่โมเดลใช้
2. **ค่าเผื่อของ `lineSpills()` ต้องมาจากเมตริกฟอนต์จริง** — `getClientRects()` คืนกล่องบรรทัด
   ที่สูงกว่า `line-height` ได้ (วัดจริง `h=21.0 lh=16px`) · ค่าเผื่อ 2px ตายตัวจึงฟ้องผิดตลอด
3. **`[79-5]` ที่ alpha.111 ทำให้ไม่ทำงานมาสามรุ่น** — สั่งปิดกลุ่มผ่าน `settings.toolbar`
   ทั้งที่ปุ่มย้ายไป `settings.fmtbar` แล้ว · ไม่มีใครเห็นเพราะ e2e ตายก่อนถึงบรรทัดนั้นทุกครั้ง
   **บทเรียน: เทสที่ถูกบังด้วยความล้มเหลวก่อนหน้า = เทสที่ไม่มีอยู่จริง**

ดูรายละเอียด + ตัวเลขที่วัดได้ ใน `CHANGELOG.md` หัวข้อ **alpha.114**
